extends layout

block content
  .mainContainer
    .options
      img(src="/images/bullscows.png")
      br
      if user
        button(class="humanGameButton", onclick="onHumanGameButtonClick()") Start Game
        br
        div(id="timer")
      else
        p
          a.btn-auth.btn-facebook(href='/auth/facebook')
            | Sign in with 
            b Facebook
        p
          a.btn-auth.btn-google(href='/auth/Google')
            | Sign in with 
            b Google
      br
      br
      br
      br
      br
      br
      br
      a(href="/leaderboard")
        button LeaderBoard
    .gameContainer
      .gamePanel
        .initialPanel
          h2 Bulls and Cows
          p(class="rules")
            | Bulls and Cows Game is a game also known as MasterMind.
            | Computer selects a four digit number with all four digits being different.
            | In the current implementation number may not begin with 0.
            | "Cow" column displays total number of digits you guessed right,
            | "Bull" shows how many of those that exists were placed at the right spots.
            | You are given 10 guesses per game to try and find the secret number.
            | Take your time and try to make most of them.
        .humanGamePanel
          form(onsubmit="return false")
            input(class="humanGuessInput1", id="humanGuessInput1" type="text" maxlength=1 style="width: 15px" onkeyup="moveToNextField(1)")
            input(class="humanGuessInput2", id="humanGuessInput2" type="text" maxlength=1 style="width: 15px" onkeyup="moveToNextField(2)")
            input(class="humanGuessInput3", id="humanGuessInput3" type="text" maxlength=1 style="width: 15px" onkeyup="moveToNextField(3)")
            input(class="humanGuessInput4", id="humanGuessInput4" type="text" maxlength=1 style="width: 15px" )
            input(type="submit", value="Make Guess" class="humanGuessButton", onclick="onHumanGuessButtonClick.call(this)")
        .aiGamePanel
          | AI GAME
      .guessesPanel
